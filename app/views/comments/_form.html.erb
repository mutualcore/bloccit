<h3>Add a comment</h3>
<%= form_for [@post, Comment.new], remote: true do |f| %>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>